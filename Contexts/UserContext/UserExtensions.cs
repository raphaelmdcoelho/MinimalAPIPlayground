namespace MinimalApiPlayground.Contexts.UserContext;

public static class UserExtensions
{
    public static void AddAccountsServices(this WebApplicationBuilder builder)
    {
        // Registro de serviços
        // Resolução de DI
    }

    public static void UseAccountsContext(this WebApplication app)
    {
        // Mapeamento de Endpoints
    }
}